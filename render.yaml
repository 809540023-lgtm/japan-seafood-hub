services:
  - type: web
    name: japan-seafood-export-hub
    runtime: static
    buildCommand: "echo no build needed"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
